

services:
  backend:
    build: ./backend
    ports:
      - "8000:8080"
    volumes:
      - ./backend:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
    build: ./frontend
    ports:
      - "3030:3000"
    stdin_open: true
    tty: true
