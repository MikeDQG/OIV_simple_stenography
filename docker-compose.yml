

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    command: uvicorn main:app --port 8000

  frontend:
    build: ./frontend
    ports:
      - "3030:3000"
    stdin_open: true
    tty: true
