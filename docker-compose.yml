

services:
  backend:
    build: ./backend
    ports:
      - "8080:8000"
    volumes:
      - ./backend:/app
    command: uvicorn main:app --host 127.0.0.1 --port 8000

  frontend:
    build: ./frontend
    ports:
      - "3030:3000"
    stdin_open: true
    tty: true
